import React from 'react';


// export default function Lounch({ lounch }) {
//   const { name, flag } = country;

//   return (
//     <div className={`${css.country} ${css.border}`}>
//       <img className={css.flag} src={flag} alt={name} />
//       <span className={css.countryName}>{name}</span>
//     </div>
//   );
// }
